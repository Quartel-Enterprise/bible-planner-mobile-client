---
name: worktree-start-task
description: "Start a new task in an isolated git worktree. Shortcut for start-task with the mode already fixed to worktree — skips asking which mode to use."
---

# Start Task (Worktree)

Shortcut for `start-task` with the mode already decided.

Read [`start-task/SKILL.md`](../start-task/SKILL.md) and follow it exactly, with one change: in
step 1, don't check the request or ask the user which mode to use — the mode is **worktree**. Skip
straight to step 2, and in step 4 follow only the "Worktree mode" section.

Do not duplicate `start-task`'s steps here; this file only fixes the mode.
