---
name: in-place-start-task
description: "Start a new task directly in the current checkout, no worktree. Shortcut for start-task with the mode already fixed to in-place — skips asking which mode to use."
---

# Start Task (In-place)

Shortcut for `start-task` with the mode already decided.

Read [`start-task/SKILL.md`](../start-task/SKILL.md) and follow it exactly, with one change: in
step 1, don't check the request or ask the user which mode to use — the mode is **in-place**. Skip
straight to step 2, and in step 4 follow only the "In-place mode" section.

Do not duplicate `start-task`'s steps here; this file only fixes the mode.
