package com.quare.bibleplanner.feature.read.presentation.screen.content

/** Which end of the list a chapter placeholder stands at, so both can be on screen at once. */
internal enum class ChapterShimmerPosition {
    LEADING,
    TRAILING,
}
