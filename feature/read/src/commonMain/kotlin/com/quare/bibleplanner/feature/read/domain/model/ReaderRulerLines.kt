package com.quare.bibleplanner.feature.read.domain.model

/** How many lines of text the reading ruler's band covers. */
object ReaderRulerLines {
    const val MIN = 1
    const val MAX = 4
    const val DEFAULT = 1
}
