package com.quare.bibleplanner.feature.read.domain.model

/** The two mutually exclusive focus aids the reader offers, plus the option of using neither. */
enum class ReaderFocusAid {
    NONE,
    RULER,
    FOCUSED_VERSE,
}
